name: Backend CD

on:
  push:
    branches:
      - server-backend
      - feature/message-broker 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Write .env file
        run: | 
            echo "${{ secrets.ENV_FILE }}" > .env
      
      - name: Write .pem files
        run: |
            mkdir ./internal/jwt
            echo "${{ secrets.PRIVATEJWTKEY }}" > ./internal/jwt/private_key.pem
            echo "${{ secrets.PUBLICJWTKEY }}" > ./internal/jwt/public_key.pem
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PAT }}
      - name: Build and Push Docker Image
        run: |
          docker compose build
          docker compose push

      - name: Copy docker-compose
        uses: appleboy/scp-action@v0.1.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "docker-compose.yaml"
          target: "/home/ubuntu/DyslexicsClock/backend"

          #FIX: has permission issues. it can't override or even delete
      # - name: Clean and copy configs
      #   uses: appleboy/ssh-action@v0.1.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     password: ${{ secrets.SERVER_PASSWORD }}
      #     script: |
      #       # Remove existing config directory
      #       # rm -rf /home/ubuntu/DyslexicsClock/backend/config
      #       # Create fresh directory
      #       mkdir -p /home/ubuntu/DyslexicsClock/backend/config/mosquitto
      # 
      # - name: Copy configs
      #   uses: appleboy/scp-action@v0.1.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     password: ${{ secrets.SERVER_PASSWORD }}
      #     source: "config/*"
      #     target: "/home/ubuntu/DyslexicsClock/backend"
      #     overwrite: true
      #
      # - name: Fix permissions
      #   uses: appleboy/ssh-action@v0.1.10
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     password: ${{ secrets.SERVER_PASSWORD }}
      #     script: |
      #       chmod 755 /home/ubuntu/DyslexicsClock/backend/config/mosquitto
      #       chmod 644 /home/ubuntu/DyslexicsClock/backend/config/mosquitto/mosquitto.conf

      - name: Deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /home/ubuntu/DyslexicsClock/backend
            echo "${{ secrets.ENV_FILE }}" > .env
            chmod 644 .env
            
            docker container prune
            docker compose pull
            docker compose down --remove-orphans
            docker compose up -d
            docker image prune -f
